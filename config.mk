# motd.sh
VERSION = 1.0

# If you prefer to install motd.sh into your home directory,
# uncomment the following line and comment out the next one.
# PREFIX = ${HOME}/.local
PREFIX = /usr/local

CONFDIR = ${PREFIX}/etc
BASEDIR = ${PREFIX}/share/motd.sh
MODULES = ${BASEDIR}/modules
